# Models Directory

This directory stores serialized models and their metadata.

Current contents:

- `lr_calibrated_triage_model.joblib` and its metadata are the current primary inference artifacts,
- `rf_calibrated_triage_model.joblib` is preserved for comparison,
- `metadata/` stores thresholds and training metadata used by the inference layer.
