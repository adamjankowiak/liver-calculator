# Legacy Research

This directory preserves the original project history:

- experimental scripts,
- archived reports and plots,
- older modeling approaches,
- intermediate analytical work.

Nothing inside `legacy/` should be treated as stable application code. It exists to preserve prior work while the main repository evolves into a cleaner ML engineering layout.
