# Data Directory

The original dataset tree has been retained and moved under `data/` to match common data science project conventions.

Current state:

- historical folders such as `Original`, `Modified`, and calculator-specific subsets are still preserved,
- no destructive cleanup was performed on the source data,
- future work should standardize the flow into `raw`, `interim`, and `processed` zones.
